export default function Loading () {
    return (
        <main class="w-full max-w-[50em] p-[16px] m-auto h-[100vh] flex justify-center items-center">
            <h1 class="font-bold text-[30px]">
                Carregant
            </h1>
        </main>
    )
}